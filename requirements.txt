# The terminal game (terminal/hangman.py) needs NOTHING extra - just Python.
#
# The clickable window game (gui/hangman_gui.py) needs pygame.
# Install it with:   pip install -r requirements.txt

pygame-ce>=2.5
